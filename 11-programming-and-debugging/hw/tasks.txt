===================
Задание 1
Выполнить задание по сетям.
Стартануть чат-бота на сокетах
Перехватить трафик с помощью tcpdump и с помощью wireshark. Записать короткое "видео с экрана" как вы это делали
Перехватить трафик с помощью tcpdump и сохранить его в pcap файл. Pcap файл прочитать wireshark, прислать мне дамп трафика
===================
Задание 2
Написать маленькую утилиту, которая в качестве аргумента принимает путь до директории и sha256 hash

Утилита должна пройти по всем файлам внутри директории и вывести в stdout абсолютный путь до файлов,
хеш которых указан в качестве аргумента

Определить:
1) какой системный вызов будет использоваться чаще всего (strace)	
2) какой участок кода "самый горячий" (профилирование)
3) какой системный вызов потребил больше всего времени (strace + анализ логов)
===================
Задание 3
Сделать так, чтобы скрипт заработал
https://github.com/budurli/EpamPython2019/blob/master/10-programming-and-debugging/hw/hw.py
Написать мне, что вы для этого сделали
===================
Задание 4 - проектирование
Вариант 7
Необходимо настроить CI для разрабатываемого мобильного приложения + back end.
    Заказчик настаивает на том, чтобы мы по возможности использовали open source software.

===================
Задание 5 - проектирование CLI утилиты
Спроектировать CLI интерфейс для указанной утилиты.
Реализовать парсинг аргументов с помощью argparse или getopt
Обязательное условие -- вменяемый help usage, который можно получить с 
    помощью ключей -h/--help

Вариант 2
CLI утилита, помогающая data-scientis-ам тренировать модели
    Пользователь должен иметь возможность задать название модели (решающее дерево,
                                                                    лес,
                                                                    градиентный бустинг,
                                                                    метод главных компонент)
    Вне зависимости от названия модели пользователь должен:
        иметь возможность указать место
            input dataset. Это может быть папка или файл в формате .csv, .parquet (это может быть и папка),
            .json, .tar.gz, .tar.bz2, .zip
        иметь возможность указать место , где будет складироваться обученная модель (локальная папка или
            валидный s3 url)
        опционально должна быть возможность указать алгоритм сжатия обученной модели

    Для каждой модели харакетрны свои параметры, управляющие процессом обучения. 
    Вы должны сами их найти где-либо
